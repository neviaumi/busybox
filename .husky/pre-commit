#!/bin/sh

set -e
npx eslint --ignore-pattern packages/ --ignore-pattern package-lock.json .
npx lerna exec --stream --since HEAD --exclude-dependents -- "test ! -f  scripts/pre-commit.sh || bash \
scripts/pre-commit.sh"
